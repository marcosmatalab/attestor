# docs/

Documentation assets for the root README.

## `dashboard.png` (to be added)

The root README references `docs/dashboard.png`. It is **not committed yet** — capture it
from the running dashboard and add it here, so nothing in the repo is a fabricated mockup.

To capture it:

1. Start the backend and frontend (see [Run it locally](../README.md#run-it-locally)).
2. Open http://localhost:3000, classify a high-risk provider, and run the end-to-end demo.
3. Screenshot the results and save as `docs/dashboard.png`, then commit it.
